<div class="container">
  <div class="row">
    <ul class="list-group d-flex">
      <% @lists.each do |list|%>
        <li class="p-4 m-2 list-group-item">
          <%= link_to "#{list.name}", list_path(list) %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
